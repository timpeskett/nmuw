#include "Screen.h"

int getScreenWidth( AppData *ad )
{
	return DisplayWidth( ad->disp, DefaultScreen( ad->disp ) );
}


int getScreenHeight( AppData *ad )
{
	return DisplayHeight( ad->disp, DefaultScreen( ad->disp ) );
}

